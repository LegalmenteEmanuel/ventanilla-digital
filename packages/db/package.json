{
  "name": "@vd/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./password": "./src/password.ts"
  },
  "scripts": {
    "build": "prisma generate",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "generate": "prisma generate",
    "migrate": "if [ -f ../../.env ]; then set -a; . ../../.env; set +a; fi; prisma migrate dev --name init",
    "migrate:deploy": "if [ -f ../../.env ]; then set -a; . ../../.env; set +a; fi; prisma migrate deploy",
    "push": "if [ -f ../../.env ]; then set -a; . ../../.env; set +a; fi; prisma db push",
    "studio": "if [ -f ../../.env ]; then set -a; . ../../.env; set +a; fi; prisma studio",
    "seed": "node --env-file-if-exists=../../.env prisma/seed.ts",
    "lint": "prettier --check \"src/**/*.ts\" \"prisma/**/*.ts\""
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "@vd/core": "workspace:*"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "typescript": "^5.7.2"
  }
}
